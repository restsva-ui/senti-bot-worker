name = "senti-bot-worker"
main = "src/index.js"
compatibility_date = "2024-09-20"
compatibility_flags = []

# !!! Увімкнено логування — корисно поки налагоджуємо
[observability]
enabled = true

# Якщо будеш використовувати Durable Objects / D1 / KV — додамо сюди bindings