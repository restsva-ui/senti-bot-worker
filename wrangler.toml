# wrangler.toml — повний файл

name = "senti-bot-worker"
main = "src/index.ts"
compatibility_date = "2024-06-20"
logpush = true

# Публічні константи (секрети тут НЕ зберігаємо)
[vars]
API_BASE_URL = "https://api.telegram.org"

# ⬇️ ГОЛОВНЕ: прив’язка KV через код (не зникне після деплою)
[[kv_namespaces]]
binding = "KV"                                    # має збігатися з env.KV у коді
id = "7b32e2d1f60846ddb1c653eb52180bf7"           # твій реальний Namespace ID (senti-state)
# preview_id = "..."                               # (не обов’язково)