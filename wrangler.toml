name = "senti-bot-worker"
main = "src/index.js"
compatibility_date = "2025-01-10"
workers_dev = true

[vars]
TZ = "Europe/Kyiv"

# 🔗 Google Drive (вкажи це як Variable у Cloudflare → Settings → Variables)
# ID папки в Google Drive, куди складати бекапи (з URL папки після /folders/)
DRIVE_FOLDER_ID = "1lMcN4cRlYNzOU1D1y5eirq5y1W7by9T-"

# 🛡️ WEBHOOK_SECRET вже використовується у проекті (для HTTP-ручок)
# Переконайся, що значення збігається з тим, що ти використовуєш у ТГ тестах
# WEBHOOK_SECRET = "senti1984"   # (задай у Cloudflare як Variable, якщо ще не задано)

# 🔑 Секрет із JSON сервісного акаунта (додай у Cloudflare → Settings → Secrets):
# Name: GOOGLE_DRIVE_CREDENTIALS
# Value: ВЕСЬ JSON, який ти завантажив із Google Cloud (цілком, з { ... })

[[kv_namespaces]]
binding = "STATE_KV"
id = "7b32e2d1f60846ddb1c653eb52180bf7"   # senti-state

[[kv_namespaces]]
binding = "DEDUP_KV"
id = "20d81d7b68fa4ac6b9913e2fbd3465d5"   # senti-dedup

[[kv_namespaces]]
binding = "TODO_KV"
id = "497eac980691495ea2a0bc8611892d65"   # senti-cache