{
  "name": "senti-bot-worker",
  "version": "1.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --remote",
    "tail": "wrangler tail",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "3.80.0"
  }
}