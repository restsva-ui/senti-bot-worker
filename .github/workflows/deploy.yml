name: Deploy Senti Worker

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: read

env:
  SCRIPT_NAME: senti-bot-worker

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      # Перевіряємо обов'язкові секрети в GitHub (а НЕ в Cloudflare Dashboard)
      - name: Sanity check required secrets
        run: |
          : "${{ secrets.CF_API_TOKEN?CF_API_TOKEN is required (Cloudflare API Token) }}"
          : "${{ secrets.CF_ACCOUNT_ID?CF_ACCOUNT_ID is required (Cloudflare Account ID) }}"

      # Деплой без створення секретів: значення з Dashboard уже доступні воркеру
      - name: Deploy with Wrangler
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CF_API_TOKEN }}
          accountId: ${{ secrets.CF_ACCOUNT_ID }}
          command: deploy --name ${{ env.SCRIPT_NAME }}

      # (опц.) Дістанемо subdomain та зробимо health-check
      - name: Install jq (for parsing JSON)
        run: sudo apt-get update && sudo apt-get install -y jq

      - name: Get workers.dev subdomain
        id: sub
        run: |
          set -e
          resp=$(curl -fsSL \
            -H "Authorization: Bearer ${{ secrets.CF_API_TOKEN }}" \
            "https://api.cloudflare.com/client/v4/accounts/${{ secrets.CF_ACCOUNT_ID }}/workers/subdomain")
          sub=$(echo "$resp" | jq -r '.result.subdomain')
          echo "subdomain=$sub" >> "$GITHUB_OUTPUT"

      - name: Health check (non-blocking)
        run: |
          set -x
          curl -fsS "https://${{ env.SCRIPT_NAME }}.${{ steps.sub.outputs.subdomain }}.workers.dev/health" || true