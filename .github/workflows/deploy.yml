name: Deploy Worker

on:
  push:
    branches: ["main"]
  workflow_dispatch:

# щоб старі запуски не валилися "operation was canceled"
concurrency:
  group: deploy-worker-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read

    steps:
      - name: 🛠️ Checkout repository
        uses: actions/checkout@v4

      - name: 🧰 Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: ⚙️ Install Wrangler v4
        run: npm install -g wrangler@4

      - name: 🔎 Wrangler version
        run: wrangler --version

      # ⬇️ ЦЕЙ крок робить job зеленим/червоним
      - name: 🚀 Deploy to Cloudflare
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: wrangler deploy

      # необов’язково, не ламає job
      - name: 🩺 Health check (non-blocking)
        if: always()
        env:
          WORKER_HOST: ${{ secrets.WORKER_HOST }}
        run: |
          curl -sS --fail --max-time 10 "https://${WORKER_HOST}/health" || true

      # лог у чеклист — теж не ламає job
      - name: ✍️ Note deploy to Checklist (non-blocking)
        if: always()
        env:
          WORKER_HOST: ${{ secrets.WORKER_HOST }}
          WEBHOOK_SECRET: ${{ secrets.WEBHOOK_SECRET }}
          COMMIT_SHA: ${{ github.sha }}
          ACTOR: ${{ github.actor }}
          RUN_ID: ${{ github.run_id }}
          STATUS: ${{ job.status }}
        run: |
          curl -sS --max-time 10 --get "https://${WORKER_HOST}/ci/deploy-note" \
            --data-urlencode "s=${WEBHOOK_SECRET}" \
            --data-urlencode "commit=${COMMIT_SHA}" \
            --data-urlencode "actor=${ACTOR}" \
            --data-urlencode "deploy=${RUN_ID}" \
            --data-urlencode "status=${STATUS}" \
            --data-urlencode "from=github-actions" || true