name: Deploy Worker

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    permissions:
      contents: read

    steps:
      - name: 🛠️ Checkout repository
        uses: actions/checkout@v4

      - name: 🧰 Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: ⚙️ Install Wrangler v4
        run: npm install -g wrangler@4
- name: 🚀 Deploy to Cloudflare
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: wrangler deploy --package-manager npm

      # Опціонально: швидкий health-check, НЕ блокує джобу
      - name: 🔎 Health (non-blocking)
        if: always()
        env:
          WORKER_HOST: ${{ secrets.WORKER_HOST }}
        run: |
          curl -sS --max-time 10 "https://${WORKER_HOST}/health" || true