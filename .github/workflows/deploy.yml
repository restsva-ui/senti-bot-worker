name: Deploy Worker

on:
  push:
    branches: ["main"]
  workflow_dispatch:

concurrency:
  group: deploy-worker
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: 🛠️ Checkout repository
        uses: actions/checkout@v4

      - name: 🧰 Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: ⚙️ Install Wrangler (latest v4)
        run: npm i -g wrangler@latest

      - name: 🔎 Show Wrangler version
        run: wrangler --version

      - name: 🚀 Deploy to Cloudflare
        env:
          # обов'язково: токен з правами "Account → Workers Scripts → Edit" (+ KV Edit бажано)
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        run: wrangler deploy
  note:
    needs: deploy
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: ✍️ Note deploy to Checklist
        env:
          WORKER_HOST: ${{ secrets.WORKER_HOST }}       # напр. senti-bot-worker.restsva.workers.dev
          WEBHOOK_SECRET: ${{ secrets.WEBHOOK_SECRET }} # має збігатися з env.WEBHOOK_SECRET у Worker
          COMMIT_SHA: ${{ github.sha }}
          ACTOR: ${{ github.actor }}
          RUN_ID: ${{ github.run_id }}
          STATUS: ${{ needs.deploy.result }}
        run: |
          curl -sS --get "https://${WORKER_HOST}/ci/deploy-note" \
            --data-urlencode "s=${WEBHOOK_SECRET}" \
            --data-urlencode "commit=${COMMIT_SHA}" \
            --data-urlencode "actor=${ACTOR}" \
            --data-urlencode "deploy=${RUN_ID}" \
            --data-urlencode "status=${STATUS}" \
            --data-urlencode "from=github-actions" || true
